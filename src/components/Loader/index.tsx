import React from 'react';
import { LoaderWrapper, LoaderElement } from './styles';


const Loader: React.FC = () => {

	return (
		<LoaderWrapper>
			<LoaderElement />
		</LoaderWrapper>
	);
};

export default Loader;